/* taskTests.sql
 *
 * Assuming an existing tasks table
 * - add a task to a valid user/list (succeed)
 * - add a task to an non-existent user/list (FAIL)
 *
 * - update a task on a valid user/list (succeed)
 * - update a task on an invalid user/list (FAIL)
 * - update a non-existent task (FAIL)
 *
 * - delete a task on a valid user/list (succeed)
 * - delete a task on an non-existent user/list (FAIL)
 * - delete a non-existent task (FAIL)
 *
 */
